// Exemplo 5 – Estrutura de Repetição (for)

#include <stdio.h>

int main() {
    //Laço que repete de 1 até 10
    for(int i = 1; i <= 10; i++) {
        printf("%d x 2 = %d\n", i, i * 2);
    }
    return 0;

    /*
        👉 Conceito:

        Uso do laço for.

        Exemplo prático: tabuada do 2.
    */
}